"use strict";

const Task = require(`task`);

module.exports = class extends Task {

    async do {

        this._start();

        try {

            this.output = await this.f();

            this.isDone = true;

        } catch (error) {

            this.error = error;

            this.threwAnError = true;

        }

        this.broadcastChange();

    }

    };