S = require(`./SyncedTree.js`);
V = require(`./Version.js`);

newerV = V.Newer(V.oldest);

s = new S((change) => console.log(change.path, `was changed`));

s.receive({path: [5], versions: [newerV, newerV]});

s.receive({path: [], versions: [newerV]});