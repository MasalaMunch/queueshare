T = require(`./SyncedTree.js`);
V = require(`./Version.js`);

t = new (class extends T {

    _write (newChange) {

        console.log("writing to", newChange.path);

        super._write(newChange);

    }

    })();

newerV = V.Newer(V.oldest);

t.receive({path: [5], versions: [newerV, newerV]});

t.receive({path: [], versions: [newerV]});