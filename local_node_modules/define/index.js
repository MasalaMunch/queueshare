"use strict";

module.exports = (target, ...sources) => {

    for (const entry of sources.filter(Boolean).map(Object.entries).flat(1)) {

        const [key, value] = entry;

        if (target[key] === undefined) {

            target[key] = value;

        }

    }

    return target;

};