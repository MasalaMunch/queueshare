"use strict";

module.exports = (target, callback) => {

    for (const key in target) {

        if (target.hasOwnProperty(key)) {

            target[key] = callback(target[key], key, target);

        }

    }

    return target;

};