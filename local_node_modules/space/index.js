"use strict";

const assert = require(`assert`);

module.exports = class {

    constructor () {

        this._set = new Set();

    }

    reserve (something) {

        assert(!this._set.has(something));

        this._set.add(something);


    }

    };
    